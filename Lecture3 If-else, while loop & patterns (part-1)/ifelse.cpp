#include<bits/stdc++.h>
using namespace std;
int main()
{
    // int n;
    // cin>>n;
    // if(n>0)
    // cout<<"+ve\n";
    // else if(n==0)
    // cout<<"0\n";
    // else
    // cout<<"-ve\n";
    // return 0;

    int a,b;
    cin>>a>>b;
    if(a>b)
    cout<<"a is greater\n";
    else if(a==b)
    cout<<"a and b both are equal\n";
    else
    cout<<"b is greater\n";

    return 0;
}